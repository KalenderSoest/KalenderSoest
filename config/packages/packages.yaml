framework:
    csrf_protection: ~
    session: ~
    secret: '%env(APP_SECRET)%'
    property_info:
        with_constructor_extractor: false
    form:
        csrf_protection:
            enabled: true
            field_name: '_token'
